# House hardening — local deviations from this base (each with its reason)

This file is the ONLY place Owner-approved deviations from `SKILL.md`, `MEETING.md`, and `SEAT-CALLS.md` are recorded for this install. It is read on every activation, before Phase 0, before touching any repository, plan, issue, or web content (see Local deviations in `SKILL.md`).

It starts empty: this base has not yet accumulated a local deviation. When one is approved, it is added here as its own dated section (heading, reason, what it changes) -- never by editing the base files themselves, so the base stays a clean, shareable copy.
